# Scripts - السكريبتات المساعدة

هذا المجلد يحتوي على سكريبتات مساعدة لإدارة التطبيق.

## set-admin-claim.js

...existing code...

### المتطلبات

...existing code...

...existing code...

### الاستخدام

#### 1. تعيين صلاحية أدمن لمستخدم

```bash
node scripts/set-admin-claim.js set user@example.com
```

#### 2. إزالة صلاحية أدمن من مستخدم

```bash
node scripts/set-admin-claim.js remove user@example.com
```

#### 3. عرض صلاحيات مستخدم

```bash
node scripts/set-admin-claim.js view user@example.com
```

### مثال كامل

```bash
# تعيين صلاحية أدمن
$ node scripts/set-admin-claim.js set admin@taskwise.com

...existing code...
🔍 البحث عن المستخدم: admin@taskwise.com...
✓ تم العثور على المستخدم
  UID: abc123xyz
  Display Name: Admin User

📋 Custom Claims الحالية: {}

⚙️  تعيين صلاحية الأدمن...

✅ تم تعيين صلاحية الأدمن بنجاح!

⚠️  ملاحظة مهمة:
   يجب على المستخدم تسجيل الخروج ثم الدخول مرة أخرى
   حتى تُطبّق الصلاحيات الجديدة
```

### للمزيد من المعلومات

راجع [ADMIN_SETUP.md](../ADMIN_SETUP.md) للحصول على دليل كامل.
