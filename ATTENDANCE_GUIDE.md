# دليل نظام الحضور والانصراف

## 📋 نظرة عامة

تم إضافة نظام حضور وانصراف شامل يسجل كل يوم بالتاريخ والساعة على مدار الشهر، مع إمكانية عرض التفاصيل الكاملة للأدمن.

---

## 🎯 الميزات

### للموظفين (Users):
1. **تسجيل الحضور والانصراف**
   - زر "Clock In" لتسجيل وقت الحضور
   - زر "Clock Out" لتسجيل وقت الانصراف
   - عرض الحالة الحالية (حاضر / منصرف / لم يسجل)

2. **عرض السجل الشخصي**
   - عرض آخر 7 أيام من سجلات الحضور
   - عرض التاريخ، وقت الحضور، وقت الانصراف
   - حساب مدة العمل لكل يوم تلقائياً

### للأدمن (Admin):
1. **تقرير الحضور الشامل**
   - عرض سجلات كل الموظفين
   - فلترة حسب الشهر والسنة
   - فلترة حسب الموظف

2. **الإحصائيات**
   - عدد أيام الحضور لكل موظف
   - إجمالي ساعات العمل
   - متوسط ساعات العمل يومياً

3. **التصدير**
   - تصدير التقرير إلى Excel
   - يشمل كل التفاصيل (الاسم، الدور، التاريخ، الأوقات، المدة)

---

## 🔧 كيفية الاستخدام

### للموظف:

1. **تسجيل الحضور:**
   ```
   - افتح التطبيق
   - في قسم "Attendance"
   - اضغط على "Clock In"
   ```

2. **تسجيل الانصراف:**
   ```
   - في نفس القسم
   - اضغط على "Clock Out"
   ```

3. **عرض السجل:**
   ```
   - السجل يظهر تلقائياً في قسم "Last 7 Days"
   - يعرض آخر 7 أيام من سجلاتك
   ```

### للأدمن:

1. **عرض تقرير الحضور:**
   ```
   - سجل دخول كأدمن
   - انتقل إلى الصفحة الرئيسية
   - قسم "تقرير الحضور والانصراف" سيظهر تلقائياً
   ```

2. **فلترة التقرير:**
   ```
   - اختر الشهر من القائمة المنسدلة
   - اختر السنة
   - اختر الموظف (أو "الكل" لعرض الجميع)
   ```

3. **تصدير إلى Excel:**
   ```
   - اضغط على زر "تصدير Excel"
   - سيتم تنزيل ملف Excel بكل التفاصيل
   ```

---

## 📊 بنية البيانات

### سجل الحضور (Attendance Record):
```typescript
{
  id: string;              // معرف السجل
  userId: string;          // معرف الموظف
  clockIn: Timestamp;      // وقت الحضور
  clockOut: Timestamp;     // وقت الانصراف (null إذا لم ينصرف بعد)
}
```

### مثال:
```json
{
  "id": "abc123",
  "userId": "user123",
  "clockIn": "2025-10-24T08:00:00Z",
  "clockOut": "2025-10-24T17:00:00Z"
}
```

---

## 🔐 الصلاحيات

### قواعد Firestore:

```javascript
match /attendance/{attendanceId} {
  // القراءة الفردية: المالك أو الأدمن
  allow get: if isSignedIn() && (resource.data.userId == request.auth.uid || isAdmin());

  // قراءة القائمة: أي مصادق (الكود يفلتر بـ where)
  allow list: if isSignedIn();

  // الإنشاء: فقط للمالك
  allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;

  // التعديل: فقط للمالك
  allow update: if isSignedIn() && resource.data.userId == request.auth.uid;

  // الحذف: فقط للمالك
  allow delete: if isSignedIn() && resource.data.userId == request.auth.uid;
}
```

---

## 💡 ملاحظات مهمة

### للموظفين:
- ✅ يمكنك تسجيل الحضور مرة واحدة فقط في اليوم
- ✅ يجب تسجيل الانصراف قبل تسجيل حضور يوم جديد
- ✅ لا يمكن تسجيل حضور وانصراف أكثر من مرة في نفس اليوم
- ⚠️ إذا نسيت تسجيل الانصراف، لن تتمكن من تسجيل حضور جديد

### للأدمن:
- ✅ يمكنك عرض سجلات كل الموظفين
- ✅ يمكنك تصدير التقرير لأي فترة
- ✅ الإحصائيات تحسب تلقائياً
- ⚠️ يجب أن يكون لديك Custom Claim `role: "admin"` لعرض التقارير

---

## 🛠️ استكشاف الأخطاء

### المشكلة: لا يظهر قسم تقرير الحضور للأدمن

**الحل:**
1. تأكد من أن لديك Custom Claim:
   ```bash
   node scripts/set-admin-claim.js view your-email@example.com
   ```

2. إذا لم يكن موجوداً:
   ```bash
   node scripts/set-admin-claim.js set your-email@example.com
   ```

3. سجل خروج ودخول مرة أخرى

### المشكلة: لا يمكن تسجيل الحضور

**الأسباب المحتملة:**
- ✓ سجلت حضوراً بالفعل اليوم
- ✓ لم تسجل انصراف الأمس

**الحل:**
- تأكد من أنك سجلت انصراف اليوم السابق
- راجع سجل آخر 7 أيام

### المشكلة: خطأ في الصلاحيات

**الحل:**
1. تأكد من أنك مسجل دخول
2. تحقق من القواعد في Firestore
3. راجع [DEPLOYMENT.md](DEPLOYMENT.md)

---

## 📈 تحسينات مستقبلية (اختيارية)

### أفكار للتطوير:
1. **التنبيهات:**
   - إشعار عند نسيان تسجيل الانصراف
   - تذكير بتسجيل الحضور في الصباح

2. **التقارير:**
   - تقرير شهري مفصل
   - مقارنة بين الموظفين
   - رسوم بيانية للحضور

3. **المرونة:**
   - السماح بتعديل السجلات من الأدمن
   - إضافة ملاحظات للسجلات
   - تحديد مواعيد عمل مرنة

4. **التكامل:**
   - ربط مع نظام الرواتب
   - ربط مع نظام الإجازات
   - تصدير PDF بدلاً من Excel فقط

---

## 📚 الملفات ذات الصلة

- [src/components/attendance.tsx](src/components/attendance.tsx) - مكون الحضور للموظفين
- [src/components/attendance-admin.tsx](src/components/attendance-admin.tsx) - مكون تقارير الأدمن
- [firestore.rules](firestore.rules) - قواعد الأمان
- [ADMIN_SETUP.md](ADMIN_SETUP.md) - إعداد صلاحيات الأدمن

---

## ✅ تم الانتهاء!

نظام الحضور والانصراف جاهز للاستخدام! 🎉
