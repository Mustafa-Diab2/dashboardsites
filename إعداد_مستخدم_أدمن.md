# دليل إعداد مستخدم أدمن 👨‍💼

## المشكلة 🔴
حسابك الحالي (`nourkhaleds1223@gmail.com`) ليس لديه صلاحيات أدمن في النظام، لذلك لا يمكنك الوصول لقائمة المستخدمين أو الميزات الإدارية.

## الحل السريع (خطوة واحدة!) ⚡

### الطريقة 1: عبر Firebase Console (الأسهل)

1. افتح [Firebase Console](https://console.firebase.google.com)
2. اختر مشروعك: `studio-6017697584-aeed8`
3. من القائمة الجانبية، اضغط على **Firestore Database**
4. ابحث عن مجموعة `users`
5. ابحث عن الوثيقة بـ ID: `PEVTqUVMPzSTmTETWXi1Bzkvx5D2`

**إذا الوثيقة موجودة:**
- افتح الوثيقة
- أضف أو عدل حقل `role`
- اكتب القيمة: `admin`
- احفظ

**إذا الوثيقة غير موجودة:**
- اضغط **Add document**
- Document ID: `PEVTqUVMPzSTmTETWXi1Bzkvx5D2`
- أضف هذه الحقول:
  - `id` (نص): `PEVTqUVMPzSTmTETWXi1Bzkvx5D2`
  - `email` (نص): `nourkhaleds1223@gmail.com`
  - `fullName` (نص): `Nour Khaled`
  - `role` (نص): `admin`
  - `createdAt` (timestamp): الوقت الحالي
- احفظ

6. **جدد المتصفح** (أو سجل خروج ودخول)
7. ✨ الآن لديك صلاحيات كاملة!

---

### الطريقة 2: باستخدام سكريبت Node.js (للمحترفين)

1. تأكد من تثبيت Firebase Admin:
```bash
cd /home/user/studio
npm install firebase-admin --save-dev
```

2. حمّل Service Account Key:
   - Firebase Console → Project Settings → Service Accounts
   - اضغط "Generate new private key"
   - احفظ الملف باسم `serviceAccountKey.json` في المجلد الرئيسي
   - **مهم:** أضف `serviceAccountKey.json` إلى `.gitignore`

3. نفذ السكريبت:
```bash
node scripts/setup-admin.js
```

4. **جدد المتصفح**

---

## التحقق من النجاح ✅

بعد ضبط الدور:

1. **جدد المتصفح** (F5)
2. ستجد في القائمة الجانبية:
   - 📈 Reports
   - 👥 Clients
   - 👤 HR Management
3. يمكنك الآن رؤية جميع المستخدمين والميزات الإدارية

---

## الأدوار المتاحة في النظام

| الدور | الصلاحيات |
|------|-----------|
| `admin` | صلاحيات كاملة: إدارة المستخدمين، العملاء، التقارير، الموارد البشرية |
| `frontend` | مستخدم عادي: رؤية مهامه فقط |
| `backend` | مستخدم عادي: رؤية مهامه فقط |

---

## استكشاف الأخطاء 🔧

### ما زلت أرى نفس الخطأ؟

1. **امسح الكاش والكوكيز** من المتصفح
2. **سجل خروج ودخول مرة أخرى**
3. **تأكد من نشر القواعد**:
   ```bash
   firebase deploy --only firestore:rules
   ```
4. **تحقق من الوثيقة** في Firebase Console
5. **تأكد من الاسم الصحيح للحقل**: `role` (حروف صغيرة)

### أريد إضافة مستخدم أدمن آخر

كرر نفس الخطوات مع User ID مختلف من Firebase Authentication.

---

## للمستقبل: Custom Claims (اختياري)

لأداء أفضل، يمكنك استخدام Custom Claims بدلاً من قراءة الوثيقة:
- راجع [FIRESTORE_RULES_OPTIMIZATION.md](./FIRESTORE_RULES_OPTIMIZATION.md)

---

## أوامر مفيدة 📝

```bash
# نشر القواعد
firebase deploy --only firestore:rules

# تشغيل سكريبت الإعداد
node scripts/setup-admin.js

# التحقق من تسجيل الدخول
firebase login

# عرض المشاريع
firebase projects:list
```

---

## ملاحظات مهمة ⚠️

1. **لا تشارك `serviceAccountKey.json`** مع أحد
2. **أضف `serviceAccountKey.json`** إلى `.gitignore`
3. **غير الباسورد** بشكل دوري للأمان
4. **استخدم Custom Claims** في الإنتاج لأداء أفضل

---

**محتاج مساعدة؟** افتح الملفات التالية:
- [SETUP_ADMIN_USER.md](./SETUP_ADMIN_USER.md) - دليل شامل بالإنجليزية
- [FIRESTORE_RULES_OPTIMIZATION.md](./FIRESTORE_RULES_OPTIMIZATION.md) - تحسين الأداء

🎉 **بالتوفيق!**
